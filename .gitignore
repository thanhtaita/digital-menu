data_resources/
*.tar.gz
*.zip
token.txt
.env
.env.*

# Dependencies & build
node_modules/
dist/
.turbo/

# Local API uploads (dish/ingredient images)
apps/api/uploads/

# Local AI chat debug logs (prompts, responses, metadata)
apps/api/logs/

# USDA FoodData Central raw data (large third-party download, not app-owned content).
# Only resources/fdc-data/import/ (the loader tooling) is tracked - re-download the CSVs
# per CLAUDE.md's "Reference data" section / resources/fdc-data/import/load.py.
resources/fdc-data/*.csv
resources/fdc-data/*.xlsx
