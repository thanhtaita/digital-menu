/** Shared `accept` for dish/ingredient gallery uploads (images + short video). */
export const GALLERY_MEDIA_ACCEPT =
  "image/jpeg,image/png,image/webp,video/mp4,video/webm,video/quicktime";

/** Shown next to multi-select file inputs in menu builder and meta ingredients. */
export const GALLERY_MULTI_FILE_HINT =
  "Select multiple files at once (Ctrl/Cmd+click or Shift+click). Reorder with Up/Down after upload.";
