# 2026-07-12

Three unrelated changes merged to local `main` this date, each documented as its own feature:

## QR code display/download in the admin portal

- **`fc05bf9`** (17:29) — restaurant admins can now fetch and view the diner-menu QR code the API already
  generated (`GET /restaurants/:id/qr`) directly from the restaurant list, with a download link for the
  PNG.
- **`0a100cd`** (17:29) — documented the feature above in `CLAUDE.md` and the `api-routes` skill.

See [`docs/goals/restaurant-admin-portal/features/qr-code-display/`](../goals/restaurant-admin-portal/features/qr-code-display/README.md).

## Rate limiting on LLM-backed and ingredient-search endpoints

- **`cd860ea`** (17:31) — AI chat (`/chat`, `/chat/stream`), ingredient suggestions, and ingredient search
  had only session auth and no throttle. Registered `@fastify/rate-limit` globally with `global: false`
  (opt-in per route), scoped per authenticated session with a per-IP fallback.

See [`docs/goals/platform-hardening/features/llm-and-search-rate-limiting/`](../goals/platform-hardening/features/llm-and-search-rate-limiting/README.md).

## Diet-type restrictions at the dish level

- **`cc11119`** (17:36) — added `ingredients.dietTags` (migration `0011`) and an LLM-assisted tagging
  pipeline mirroring the FDC nutrition backfill, then wired `restriction-engine.ts` to actually check
  diet-type restrictions against dish ingredients - previously stored per-user but silently ignored at the
  dish level.

See [`docs/goals/dietary-safety-and-nutrition/features/diet-type-restrictions/`](../goals/dietary-safety-and-nutrition/features/diet-type-restrictions/README.md).

Not run/verified live in this environment (no reachable Postgres) - verified via unit/route tests with a
mocked db layer instead.
