# 2026-07-11

## FDC nutrition backfill

Two commits landed the USDA FoodData Central reference dataset and the backfill pipeline that consumes it:

- **`ee68ba6`** (20:26) — loaded the USDA FoodData Central "Foundation Foods" CSV export (2025-12-18, 24
  tables, ~600k rows) plus import tooling (`schema.sql`, `load.py`, smoke tests) into
  `resources/FoodData_Central_foundation_food_csv_2025-12-18/`, as a separate `fdc` Postgres schema
  alongside the app's own `public` schema.
- **`a89bde8`** (20:59) — added `services/fdc-matching.ts` (pg_trgm fuzzy match against `fdc.food`), the
  `backfill:fdc` script, `ingredients.fdcId`/`nutrients` columns (migration `0010`), an
  `ingredient_fdc_candidates` superadmin review queue, and sodium in the diner-app nutrition display.

See [`docs/goals/dietary-safety-and-nutrition/features/fdc-nutrition-backfill/`](../goals/dietary-safety-and-nutrition/features/fdc-nutrition-backfill/README.md)
and [`docs/architecture/fdc-reference-data.md`](../architecture/fdc-reference-data.md) for the full design.

Not run/verified live in this environment (no reachable Postgres/psycopg2) - verified via unit/route tests
with a mocked db layer instead.
